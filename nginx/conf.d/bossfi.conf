# Upstream for BossFi Backend
upstream bossfi_backend {
    server bossfi-backend:8080;
    keepalive 32;
}

# HTTP服务器配置
server {
    listen 80;
    listen [::]:80;
    server_name bossfi.com www.bossfi.com;
    
    # 健康检查端点
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
    
    # 重定向到HTTPS (生产环境启用)
    # return 301 https://$server_name$request_uri;
    
    # 临时用于HTTP访问（开发/测试环境）
    location / {
        proxy_pass http://bossfi_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 超时设置
        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # 限流
        limit_req zone=api burst=20 nodelay;
    }
    
    # API路由配置
    location /api/ {
        proxy_pass http://bossfi_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 超时设置
        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # API限流
        limit_req zone=api burst=50 nodelay;
    }
    
    # 登录接口特殊限流
    location /api/v1/auth/login {
        proxy_pass http://bossfi_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 登录限流
        limit_req zone=login burst=5 nodelay;
    }
    
    # Swagger文档
    location /swagger/ {
        proxy_pass http://bossfi_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # 静态文件缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        proxy_pass http://bossfi_backend;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS服务器配置（生产环境使用）
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name bossfi.com www.bossfi.com;
#     
#     # SSL证书配置
#     ssl_certificate /etc/nginx/ssl/bossfi.com.crt;
#     ssl_certificate_key /etc/nginx/ssl/bossfi.com.key;
#     
#     # SSL安全配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     
#     # HSTS
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     
#     # 其余配置与HTTP相同...
# } 